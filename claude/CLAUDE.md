# ~/.claude/CLAUDE.md

- Prefer fetching GitHub content with `wget` or `curl` rather than `gh`.

## Zig

- Where a style is not specified, use the [TigerStyle coding
  conventions][https://github.com/tigerbeetle/tigerbeetle/blob/main/docs/TIGER_STYLE.md].
